# Test Fixtures

This directory holds static fixture data for tests. Most tests build their
own temporary directories via `test/helpers/tmpdir.js`; put a file here only
when it is shared between multiple test files.
